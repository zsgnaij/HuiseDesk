{
  "name": "husie-desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node start.js",
    "postinstall": "cd packages/sg-scripts && python -m poetry install --no-root"
  },
  "workspaces": [
    "packages/*"
  ]
}
